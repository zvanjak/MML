name: Ubuntu

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: ubuntu-${{ github.ref }}
  cancel-in-progress: true

env:
  BUILD_TYPE: Release
  CCACHE_DIR: ${{ github.workspace }}/.ccache

jobs:
  build:
    name: ${{ matrix.compiler_name }} / ${{ matrix.real_type }}
    runs-on: ubuntu-latest
    timeout-minutes: 150
    strategy:
      fail-fast: false
      matrix:
        compiler_name: [GCC, Clang]
        real_type: [float, double, long_double]
        include:
          - compiler_name: GCC
            compiler: gcc
          - compiler_name: Clang
            compiler: clang

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup ccache
      run: |
        sudo apt-get update
        sudo apt-get install -y ccache
        echo "CMAKE_CXX_COMPILER_LAUNCHER=ccache" >> $GITHUB_ENV

    - name: Cache ccache
      uses: actions/cache@v4
      with:
        path: ${{ env.CCACHE_DIR }}
        key: ccache-ubuntu-${{ matrix.compiler_name }}-${{ matrix.real_type }}-${{ github.sha }}
        restore-keys: |
          ccache-ubuntu-${{ matrix.compiler_name }}-${{ matrix.real_type }}-

    - name: Setup GCC
      if: matrix.compiler == 'gcc'
      run: |
        echo "CC=gcc" >> $GITHUB_ENV
        echo "CXX=g++" >> $GITHUB_ENV

    - name: Setup Clang
      if: matrix.compiler == 'clang'
      run: |
        echo "CC=clang" >> $GITHUB_ENV
        echo "CXX=clang++" >> $GITHUB_ENV

    - name: Configure CMake
      run: |
        cmake -B build -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }} \
          -DMML_REAL_TYPE=${{ matrix.real_type }} \
          -DCMAKE_CXX_COMPILER_LAUNCHER=ccache

    - name: Build Tests
      timeout-minutes: 110
      # GCC uses significantly more memory than Clang - limit parallelism to avoid OOM
      run: |
        if [ "${{ matrix.compiler }}" = "gcc" ]; then
          cmake --build build --config ${{ env.BUILD_TYPE }} --target MML_Tests --parallel 2
        else
          cmake --build build --config ${{ env.BUILD_TYPE }} --target MML_Tests --parallel
        fi

    - name: Test
      timeout-minutes: 30
      working-directory: build
      run: ctest -C ${{ env.BUILD_TYPE }} -L core --output-on-failure --parallel --timeout 120

    - name: ccache stats
      run: ccache --show-stats

    - name: Upload test results on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: test-results-ubuntu-${{ matrix.compiler_name }}-${{ matrix.real_type }}
        path: build/Testing/Temporary/
        retention-days: 7
